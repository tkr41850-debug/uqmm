# Design

What uqmm is being built to do. Tracks the codebase; updated as design decisions land.

## Documents

- [config](config.md) — `VMConfig` schema, per-technique seed builders, lifecycle layer.
- [cli](cli.md) — command surface (`create`, `start`, `stop`, ...) and on-disk state layout.
